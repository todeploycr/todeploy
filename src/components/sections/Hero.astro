---
// HeroSection.astro
---

<section
  id="inicio"
  class="relative min-h-screen flex items-center justify-center pt-16 sm:pt-20 md:pt-24 pb-12 sm:pb-16"
>
  <!-- Gradient orbs - Responsive -->
  <div
    class="absolute top-1/4 left-1/4 w-48 h-48 sm:w-64 sm:h-64 md:w-80 md:h-80 lg:w-96 lg:h-96 bg-primary/20 rounded-full blur-[80px] sm:blur-[100px] md:blur-[120px] animate-pulse"
  >
  </div>
  <div
    class="absolute bottom-1/4 right-1/4 w-40 h-40 sm:w-56 sm:h-56 md:w-72 md:h-72 lg:w-80 lg:h-80 bg-accent/20 rounded-full blur-[60px] sm:blur-[80px] md:blur-[100px] animate-pulse animation-delay-1000"
  >
  </div>

  <div
    class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center w-full"
  >
    <div class="max-w-4xl mx-auto hero-content">
      <!-- Main heading - Responsive -->
      <h1
        class="font-heading text-3xl xs:text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold leading-tight mb-4 sm:mb-6 px-2 sm:px-0"
      >
        <span class="text-white block sm:inline"
          >De una idea simple a una
        </span>
        <span
          class="bg-clip-text text-transparent bg-linear-to-r from-[#00D6A1] to-[#00D6A1]/80 block sm:inline mt-2 sm:mt-0"
          >creación increíble</span
        >
      </h1>

      <!-- Subtitle - Responsive -->
      <p
        class="text-gray-400 text-base sm:text-lg md:text-xl max-w-2xl mx-auto mb-8 sm:mb-10 px-4 sm:px-0 leading-relaxed"
      >
        Desarrollamos software y sitios web a medida, enfocados en crear
        experiencias digitales sólidas y escalables. Automatizamos procesos e
        integramos inteligencia artificial para optimizar la operación de tu
        negocio.
      </p>

      <!-- CTA Buttons - Responsive -->
      <div
        class="flex flex-col sm:flex-row items-stretch sm:items-center justify-center gap-3 sm:gap-4 cta-buttons px-4 sm:px-0"
      >
        <a
          href="#contacto"
          class="bg-[#00D6A1] hover:bg-[#00D6A1]/90 text-[#0a0b1a] font-semibold px-6 sm:px-8 py-3 sm:py-4 md:py-5 text-base sm:text-lg rounded-lg transition-all group inline-flex items-center justify-center shadow-[0_0_30px_hsl(165_100%_42%/0.3)] hover:shadow-[0_0_40px_hsl(165_100%_42%/0.4)] active:scale-95"
        >
          <span>Comenzar Proyecto</span>
          <svg
            class="ml-2 w-4 h-4 sm:w-5 sm:h-5 group-hover:translate-x-1 transition-transform"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </a>
        <a
          href="#portfolio"
          class="text-white border-2 border-white/20 hover:bg-[#ed1d85] hover:border-[#ed1d85] hover:shadow-[0_0_30px_hsl(330_81%_53%/0.3)] px-6 sm:px-8 py-3 sm:py-4 md:py-5 text-base sm:text-lg rounded-lg transition-all bg-transparent active:scale-95 hover:text-white"
        >
          Ver Nuestro Trabajo
        </a>
      </div>

      <!-- Scroll Indicator - Hidden on mobile -->
      <div
        class="hidden sm:flex justify-center mt-16 md:mt-20 scroll-indicator"
      >
        <div class="flex flex-col items-center gap-2">
          <span class="text-xs text-gray-500 uppercase tracking-wider"
            >Scroll</span
          >
          <div
            class="w-6 h-10 border-2 border-gray-600 rounded-full flex items-start justify-center p-2"
          >
            <div class="w-1.5 h-1.5 bg-[#00D6A1] rounded-full scroll-dot"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animation delay utility */
  .animation-delay-1000 {
    animation-delay: 1s;
  }

  /* Fade in animations */
  .hero-content {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .badge-animate {
    animation: fadeInScale 0.6s ease-out 0.2s forwards;
    opacity: 0;
  }

  .cta-buttons {
    animation: fadeInUp 0.6s ease-out 0.4s forwards;
    opacity: 0;
  }

  .stats-grid {
    animation: fadeIn 0.6s ease-out 0.6s forwards;
    opacity: 0;
  }

  .scroll-indicator {
    animation: fadeIn 0.6s ease-out 1s forwards;
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes fadeInScale {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  /* Scroll indicator animation */
  .scroll-dot {
    animation: scrollBounce 1.5s ease-in-out infinite;
  }

  @keyframes scrollBounce {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(8px);
    }
  }

  /* Stat items fade in */
  .stat-item {
    animation: fadeIn 0.6s ease-out forwards;
    opacity: 0;
  }

  /* Mobile optimizations */
  @media (max-width: 640px) {
    .hero-content {
      animation: fadeInUp 0.6s ease-out forwards;
    }

    .cta-buttons {
      animation: fadeInUp 0.5s ease-out 0.3s forwards;
    }
  }

  /* Prevent text overflow */
  h1 {
    overflow-wrap: break-word;
    word-wrap: break-word;
    hyphens: auto;
  }

  /* Touch targets for mobile */
  @media (max-width: 640px) {
    a,
    button {
      min-height: 44px; /* Apple recommended touch target */
    }
  }

  /* Gradient text fix for Safari */
  .bg-clip-text {
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>
